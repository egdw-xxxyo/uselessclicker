package org.dikhim.jclicker.eventmanager.listener;

public abstract class SimpleMouseListener extends SimpleListener implements MouseListener {
    public SimpleMouseListener(String id) {
        super(id);
    }
}
