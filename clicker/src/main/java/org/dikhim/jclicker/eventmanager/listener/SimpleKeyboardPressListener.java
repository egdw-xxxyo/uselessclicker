package org.dikhim.jclicker.eventmanager.listener;

public abstract class SimpleKeyboardPressListener extends SimpleListener implements KeyPressListener {
    public SimpleKeyboardPressListener(String id) {
        super(id);
    }
}
