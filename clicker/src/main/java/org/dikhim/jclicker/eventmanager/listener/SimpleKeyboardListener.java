package org.dikhim.jclicker.eventmanager.listener;

public abstract class SimpleKeyboardListener extends SimpleListener implements KeyListener {
    public SimpleKeyboardListener(String id) {
        super(id);
    }
}
