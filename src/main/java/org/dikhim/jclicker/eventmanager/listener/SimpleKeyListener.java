package org.dikhim.jclicker.eventmanager.listener;

public abstract class SimpleKeyListener extends SimpleListener implements KeyListener {
    public SimpleKeyListener(String id) {
        super(id);
    }
}
