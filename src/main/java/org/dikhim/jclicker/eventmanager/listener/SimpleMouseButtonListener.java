package org.dikhim.jclicker.eventmanager.listener;

public abstract class SimpleMouseButtonListener extends SimpleListener implements MouseButtonListener {
    public SimpleMouseButtonListener(String id) {
        super(id);
    }
}
