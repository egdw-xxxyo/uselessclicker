package org.dikhim.jclicker.eventmanager.listener;

public abstract class SimpleKeyPressListener extends SimpleListener implements KeyPressListener {
    public SimpleKeyPressListener(String id) {
        super(id);
    }
}
