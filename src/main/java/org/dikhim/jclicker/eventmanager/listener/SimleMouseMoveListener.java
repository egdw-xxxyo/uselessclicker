package org.dikhim.jclicker.eventmanager.listener;

public abstract class SimleMouseMoveListener extends SimpleListener implements MouseMoveListener {
    public SimleMouseMoveListener(String id) {
        super(id);
    }
}
