package org.dikhim.jclicker.eventmanager.listener;

public abstract class SimpleMouseReleaseListener extends SimpleListener implements MouseReleaseListener {
    public SimpleMouseReleaseListener(String id) {
        super(id);
    }
}
